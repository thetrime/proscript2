Modules
Other types: (parsing, passing them in to predicates and handling where appropriate)
   bigintegers
   rationals
inria-suite
tail recursion
setup-call-cleanup
LRU cache for atoms/integers/floats
documenting the design
Foreign exception handling
Metapredicates
Convert stream code to use buffers everywhere

tidy
   There are at least 2 representations of every instruction(!)
Checkings
   There are many CHECKME and FIXMEs in the code


Bugs:
   It seems suspicious that hVar doesn't do anything (?!)
   If we retract a clause from a predicate that we are executing, then the code in the stack will be wrong. How can this possibly be avoided, though?