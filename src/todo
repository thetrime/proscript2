Modules
Foreign Predicates
catch/3
Other types:
   floats
   bigintegers
   rationals
inria-suite
tail recursion
LRU cache for atoms/integers/floats
documenting the design
ISO predicates (and others)
tidy
   There are at least 2 representations of every instruction(!)
Checkings
   There are many CHECKME and FIXMEs in the code


Bugs:
   _ is interpreted as the same variable by the parser. Thus foo(_,_) compiles to h_void,h_var rather than h_void,h_void