directives
modules
format/3
meta-predicates
emscripten

Concerns
I'm not sure that the stack is safe. The only space allocated is sizeof(frame). The slots dont take up any space, which means the next frame just clobbers them(?!)